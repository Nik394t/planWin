services:
  plan_win_bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - DB_PATH=/data/bot.db
    volumes:
      - ./data:/data
      - ./exports:/app/exports
    restart: unless-stopped
